[Unit]
Description=gunicorn daemon
After=network.target

[Service]
Type=simple
User=jarvis
WorkingDirectory={{ proj_dir }}
ExecStart={{ venv_dir }}/venv/bin/gunicorn -b 127.0.0.1:{{ port }} {{ app_name }}.wsgi

[Install]
WantedBy=multi-user.target
